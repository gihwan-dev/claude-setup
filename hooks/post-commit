#!/usr/bin/env bash
# Auto-run install_assets.py after commit if relevant files changed.

source "$(dirname "$0")/_lib.sh"

# For initial commit where HEAD~1 doesn't exist, fallback triggers install.
claude_setup_auto_install "HEAD~1" "HEAD"
