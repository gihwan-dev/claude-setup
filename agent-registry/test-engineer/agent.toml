id = "test-engineer"
role = "reviewer"
description = "Read-only reviewer for test quality, AI-generated test detection, and regression risk."
source = "codex-builtin"

[projection]
repo = true
codex = true

[repo]
model = "sonnet"
tools = ["Read", "Grep", "Glob"]

[codex]
agent_key = "test-engineer"
config_file = "test-engineer.toml"
model = "gpt-5.4"
reasoning_effort = "xhigh"
sandbox_mode = "read-only"
