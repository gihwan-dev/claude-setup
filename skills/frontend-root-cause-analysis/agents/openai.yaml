interface:
  display_name: "Frontend Root Cause Analysis"
  short_description: "Analyze frontend bugs with Five Whys and evidence-driven root-cause reasoning"
  default_prompt: "$frontend-root-cause-analysis Analyze this frontend bug by root cause instead of proposing a symptom-only patch"
policy:
  allow_implicit_invocation: false
