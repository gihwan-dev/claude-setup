interface:
  display_name: "Multi Work"
  short_description: "Manager-style orchestration utility for helper fan-out, orchestration strategy synthesis, and bounded work decomposition"
  default_prompt: >
    $multi-work — execute manager-style multi-agent orchestration per SKILL.md.
    Read routing-contract.md, dispatch helpers, synthesize an Orchestration Strategy.
policy:
  allow_implicit_invocation: false
