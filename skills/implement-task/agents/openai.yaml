interface:
  display_name: "Implement Task"
  short_description: "Execute next slice by default or run fresh-writer slice loop, then update STATUS.md"
  default_prompt: "$implement-task tasks/<task-slug>/PLAN.md 기준으로 다음 slice 하나를 구현하고 STATUS.md를 갱신해"
policy:
  allow_implicit_invocation: true
